<!-- we will render seperate nav bars dependng on f the user is signed in or not. -->
<% if !user_signed_in? %>
 <%= render '/layouts/nav_user' %>
<% else %>
 <%= render '/layouts/nav_guest' %>
<% end %>